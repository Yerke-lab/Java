# Используем базовый образ с JDK
FROM openjdk:11 AS compiler

# Устанавливаем директорию приложения в образе
WORKDIR /app

# Копируем исходный код Java-проекта в рабочую директорию образа
COPY ./src /app/src

# Компилируем Java-проект и сохраняем скомпилированные классы в отдельную директорию
RUN javac -d /app/bin /app/src/*.java

