# Используем базовый образ с JRE
FROM openjdk:11-jre

# Устанавливаем директорию приложения в образе
WORKDIR /app

# Копируем скомпилированные классы Java-проекта из образа компиляции в текущий образ
COPY --from=compiler /app/bin /app/bin

# Запускаем выполнение Java-проекта
CMD ["java", "-cp", "/app/bin", "Main"]
